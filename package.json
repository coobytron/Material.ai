{
  "name": "material-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "//": "No runtime dependencies. Ollama is accessed through its local HTTP API; static mode uses the deterministic demo engine.",
  "engines": {"node": ">=20"}
}
