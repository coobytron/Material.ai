{
  "name": "material-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "//": "No runtime dependencies. The app runs fully offline. Claude mode is opt-in: npm install @anthropic-ai/sdk",
  "engines": {"node": ">=20"}
}
